//fill in aws values. I'm not committing them though.
//authorization node error with mysql 8 unless you create a user 
//with standard access encryption (and assign roles): 
module.exports = {
    host: '',
    user: '', 
    password: '!',
    database: '',
    port: '3306'
  };
    